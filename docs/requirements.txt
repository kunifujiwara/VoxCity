myst-nb
sphinx-autoapi
furo
sphinxcontrib-bibtex
sphinx
sphinx-design
# Note: the voxcity package itself is installed separately (see
# .readthedocs.yml `python.install: path: .` and the docs workflow), not via
# an editable "-e ." entry here, so Dependabot can update these doc deps.